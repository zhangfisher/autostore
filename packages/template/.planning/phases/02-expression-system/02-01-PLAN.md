---
phase: 02-expression-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types.ts
  - src/ExpressionParser.ts
  - src/Binding.ts
  - src/__tests__/ExpressionParser.test.ts
  - src/__tests__/Binding.test.ts
autonomous: true
requirements:
  - EXPR-01
  - EXPR-02
  - EXPR-03
tags: [expression, parser, security, binding, computed]

must_haves:
  truths:
    - "ExpressionParser 能解析标识符访问表达式（如 user.name）并返回求值函数和依赖路径（EXPR-01）"
    - "ExpressionParser 能解析字面量表达式（'hello', 123, true, false）且字面量不产生依赖（EXPR-02）"
    - "ExpressionParser 拒绝不安全表达式（函数调用、赋值、成员构造等）并抛出 ExpressionParseError（EXPR-03）"
    - "Binding 构造函数接收 (el, directiveName, expression, store, directiveRegistry) 并内部查找指令对象（D-06/D-07）"
    - "Binding 内部调用 store.computedObjects.create() 创建计算对象并自动追踪依赖（D-08/D-29/D-30）"
    - "Binding 监听计算对象变化并调用 directive.update(el, value) 触发 DOM 更新（D-09/D-10）"
  artifacts:
    - path: "src/ExpressionParser.ts"
      provides: "安全的表达式解析与求值器（标识符访问 + 字面量 + 安全校验，D-04 桥接 AutoStore 计算属性）"
      exports: ["ExpressionParser", "ExpressionParseError"]
      min_lines: 120
    - path: "src/Binding.ts"
      provides: "连接 {el, directive, computed} 的绑定类（D-05~D-11，内部查找指令+创建计算对象+监听+触发更新）"
      exports: ["Binding"]
      min_lines: 90
    - path: "src/types.ts"
      provides: "扩展 DirectiveUpdater 类型（D-17/D-18）"
  key_links:
    - from: "src/Binding.ts"
      to: "store.computedObjects.create()"
      via: "Binding 内部创建 AutoStore 计算对象"
      pattern: "computedObjects\\.create"
    - from: "src/Binding.ts"
      to: "src/ExpressionParser.ts"
      via: "Binding 用 ExpressionParser 解析表达式字符串为求值函数"
      pattern: "ExpressionParser"
    - from: "src/Binding.ts"
      to: "computed.watch()"
      via: "监听计算对象值变化触发指令更新"
      pattern: "\\.watch\\("
---

<objective>
实现 Phase 02 表达式系统的第一层：安全的表达式解析器（ExpressionParser）与简化后的 Binding 类。

Purpose: CONTEXT.md 已更新为简化设计——**移除 ExpressionBridge 中间层**（D-11），Binding 直接负责：(1) 根据 directiveName 从 DirectiveRegistry 查找指令对象（D-07）；(2) 用 ExpressionParser 把表达式字符串安全解析为求值函数（EXPR-01/02/03 + D-04）；(3) 调用 `store.computedObjects.create()` 创建计算对象，由 AutoStore 自动追踪依赖与缓存（D-02/D-08/D-29/D-30）；(4) 监听计算对象变化并触发 `directive.update(el, value)`（D-09/D-10）。ExpressionParser 仍是必需的——它负责把 `'user.name'` 字符串安全地转换为 `(scope) => scope.user.name` 求值函数（不能直接把字符串当代码），并校验安全性。

Output: ExpressionParser.ts、Binding.ts、types.ts 扩展、以及两组单元测试。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-expression-system/02-CONTEXT.md
@.planning/phases/01-core-infrastructure/01-01-SUMMARY.md

<interfaces>
<!-- Phase 01 已建立的契约，executor 直接使用，无需探索代码库 -->

From src/types.ts (Phase 01 现有，本计划将扩展):
```typescript
export type AnyAutoStore = AutoStore<any, any>;

export interface Directive {
    name: string;
    priority: number;
    init(el: HTMLElement, binding: DirectiveBinding, store: AnyAutoStore): void | (() => void);
    update?(el: HTMLElement, binding: DirectiveBinding, store: AnyAutoStore): void;
    destroy?(el: HTMLElement): void;
}

export interface DirectiveBinding {
    directive: string;
    expression: string;
    element: HTMLElement;
    parsed: ParsedExpression | null;
}

export interface ParsedExpression {
    dependencies: string[];
    evaluate(scope: any): any;
}
```

From src/DirectiveRegistry.ts (Phase 01 现有):
```typescript
export class DirectiveRegistry {
    get(name: string): Directive | undefined;
    has(name: string): boolean;
    registerDirective(name: string, directive: Directive): void;
}
```

From AutoStore core (../../core/src/computed/computedObjects.ts) — 动态创建计算对象的真实签名:
```typescript
class ComputedObjects<State> extends Map<string, ComputedObject> {
    // 同步计算对象：getter 内对 scope 的读取会被 AutoStore 自动追踪为依赖
    create<Value, Scope>(
        getter: ComputedGetter<Value, Scope>,
        options?: SyncComputedOptions<Value, Scope>,
    ): SyncComputedObject<Value, Scope>;
    delete(id: string): boolean;  // 调用 computedObject.detach() 并从 Map 移除
}
```

From AutoStore core (../../core/src/observer/observer.ts) — SyncComputedObject 继承 ObserverObject:
```typescript
class ObserverObject {
    get id(): string;
    get value(): Value;           // 当前计算结果
    get path(): string[];
    watch(listener: (operate: StateOperate) => void, options?): Watcher;  // 监听值变化，返回 Watcher
    detach(): void;                // 解绑所有订阅者（清理）
}
// SyncComputedObject 通过 onInitial() -> collectDependencies() 自动收集依赖：
// 首次运行 getter 时，对 shadowStore 的 get 事件进行监听，
// getter 内对 scope.user.name 的读取会被记录为依赖 ['user','name']，
// 然后 attach() 建立 write 监听，依赖变化时自动 run() 重算。
// getter 的 scope 参数是根状态的响应式代理（scope 默认 "ROOT"）。
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 扩展类型定义并实现 ExpressionParser 安全求值器</name>
  <files>src/types.ts, src/ExpressionParser.ts, src/__tests__/ExpressionParser.test.ts</files>

  <read_first>
    - src/types.ts（当前 ParsedExpression / DirectiveBinding / Directive 定义，扩展而非重写）
    - .planning/phases/02-expression-system/02-CONTEXT.md（D-04 表达式=AutoStore 计算属性、D-22~D-24 容错模式、D-17/D-18 更新器对象）
    - ../../core/src/computed/sync.ts（sync 计算对象如何通过 getter 内 scope 读取自动收集依赖——求值器需返回值供 getter 使用，scope 是根状态响应式代理）
  </read_first>

  <behavior>
    标识符访问（EXPR-01）:
    - parseAndEval("user.name", {user:{name:"zhang"}}) 返回 "zhang"
    - parseAndEval("items.length", {items:[1,2,3]}) 返回 3
    - parseAndEval("a.b.c", {a:{b:{c:42}}}) 返回 42
    - getDependencies("user.name") 返回 ["user.name"]（点号路径合并为完整依赖路径字符串）
    - getDependencies("a.b.c") 返回 ["a.b.c"]
    - 依赖变化时由 AutoStore 计算对象触发重算（在 Task 2 Binding 中端到端验证）

    字面量（EXPR-02）:
    - parseAndEval("'hello'", {}) 返回 "hello"（单引号字符串）
    - parseAndEval('"world"', {}) 返回 "world"（双引号字符串）
    - parseAndEval("123", {}) 返回 123（数字）
    - parseAndEval("true", {}) 返回 true
    - parseAndEval("false", {}) 返回 false
    - getDependencies("'hello'") 返回 []（字面量不产生依赖）
    - getDependencies("123") 返回 []

    安全性（EXPR-03）——以下全部抛出 ExpressionParseError:
    - 含 "function" 关键字的函数表达式
    - 含 "=>" 箭头函数
    - 含 "new " 构造调用
    - 含赋值 "="（除 "==" 和 "===" 和 "!=" 和 "!==" 和 "<=" 和 ">="）
    - 含 ";" 多语句
    - 含 "(" 或 ")" 函数调用语法
    - 含 "[" 或 "]" 索引访问（v1 限制，用点号路径）
    - parse("") 抛出 ExpressionParseError（空表达式）

    容错（D-22~D-24）:
    - parse 提供 throws 选项：当 { throws: false } 时返回 null 而非抛出（供 Binding 容错使用）
  </behavior>

  <action>
    1. 扩展 src/types.ts：
       - 新增 `export class ExpressionParseError extends Error`，构造函数接收 message，name 固定为 "ExpressionParseError"。
       - 新增类型 `ExpressionOptions`：`{ throws?: boolean; dependencies?: string[] }`（供 parse 选项，对应 D-22~D-24 容错）。
       - 保留现有 ParsedExpression 接口不变（dependencies: string[] + evaluate(scope)），本任务产出符合该接口的对象。
       - 切勿在本任务定义 DirectiveUpdater 类型——简化设计下指令仍用 Directive 接口的 update/destroy 方法（D-17/D-18 的"更新器对象"概念由 Directive 接口承载，Binding 直接调用 directive.update/directive.destroy）。若 executor 认为有必要为更新器对象单独定义接口，留给 Plan 02-02 集成时决定，本任务不引入。

    2. 创建 src/ExpressionParser.ts，导出 `ExpressionParser` 类（单例风格，提供静态方法或实例方法均可）：
       - 方法 `parse(expr: string, options?: ExpressionOptions): ParsedExpression | null`：返回 { dependencies, evaluate }。
       - 方法 `getDependencies(expr: string): string[]`：提取标识符路径。
       - 方法 `parseAndEval(expr: string, scope: any, options?: ExpressionOptions): any`：便捷方法，解析并求值。
       - 实现【受限求值器】策略，不使用 eval/new Function（这会违反 EXPR-03）：
         a. 字面量识别：用正则匹配单/双引号字符串、纯数字、true/false。字面量 dependencies 为 []，evaluate 返回字面量值（字符串去引号、数字 Number()、布尔解析）。
         b. 标识符路径识别：用正则 `/^[A-Za-z_$][\w$]*(?:\.[A-Za-z_$][\w$]*)*$/` 校验整串仅为点号路径。
         c. 路径求值：evaluate(scope) 按 "." 拆分路径，逐层读取 scope。缺失中间节点返回 undefined（不抛错）。
         d. 安全校验（EXPR-03）：在 parse 入口先跑 rejectUnsafe(expr)，用正则/blacklist 检测：`/\bfunction\b/`、`/=>/`、`/\bnew\s+/`、`/[;]/`、`/[\(\)\[\]]/`、以及赋值 `/(?<![=!<>])=(?![=])/`（排除 == === != !== <= >=）。命中即抛 ExpressionParseError。
       - 容错选项（D-22~D-24）：options.throws === false 时，parse 捕获错误返回 null（不抛出），由调用方（Binding）决定是否 warn。默认 throws=true 保持原抛出语义。

    3. 创建 src/__tests__/ExpressionParser.test.ts，用 bun:test 的 describe/it/expect 覆盖 <behavior> 中所有断言（标识符、字面量、安全性、容错）。测试名称使用中文（遵循项目约定）。

    切勿：不要在 ExpressionParser 内调用 AutoStore 或 computedObjects（本任务纯解析层，AutoStore 集成在 Task 2 Binding 内部）。不要修改 AutoTemplate.ts / TemplateScanner.ts（Plan 02-02 负责）。不要创建 ExpressionBridge 类（D-11 已移除该中间层）。
  </action>

  <verify>
    <automated>cd packages/template && bun test src/__tests__/ExpressionParser.test.ts</automated>
  </verify>

  <done>
    - src/ExpressionParser.ts 存在且导出 `class ExpressionParser` 与 `class ExpressionParseError`
    - src/types.ts 包含 `type ExpressionOptions` 与 `class ExpressionParseError`（或在 ExpressionParser.ts 中导出）
    - bun test 通过，覆盖：user.name→"zhang"、items.length→3、'hello'→"hello"、123→123、true→true、字面量 dependencies 为 []、标识符 dependencies 含完整点号路径
    - 安全性用例全部抛出 ExpressionParseError：含 function/=>/new/;/()[]/赋值=
    - parse("") 抛出 ExpressionParseError；parse(x, {throws:false}) 对非法输入返回 null
    - `grep -c "new Function\|eval(" src/ExpressionParser.ts` 输出 0（禁止动态代码执行）
    - TypeScript 编译无错误：cd packages/template && bunx tsc --noEmit
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 实现简化版 Binding 类（内部查找指令+创建计算对象+监听+触发更新）</name>
  <files>src/Binding.ts, src/__tests__/Binding.test.ts</files>

  <read_first>
    - src/ExpressionParser.ts（Task 1 产出，调用其 parse 方法获取 ParsedExpression）
    - src/types.ts（AnyAutoStore、Directive、DirectiveRegistry、ParsedExpression、ExpressionOptions）
    - src/DirectiveRegistry.ts（Phase 01，get(name) 方法签名）
    - ../../core/src/computed/computedObjects.ts（create(getter, options?) 签名 + delete(id) 清理）
    - ../../core/src/observer/observer.ts（SyncComputedObject 继承 ObserverObject：watch(listener) 监听值变化返回 Watcher、detach() 清理、value getter 取当前值、id 属性）
    - ../../core/src/computed/sync.ts（onInitial -> collectDependencies 自动追踪依赖：getter 内对 scope 的读取被记录为依赖）
    - .planning/phases/02-expression-system/02-CONTEXT.md（D-05~D-11 简化设计、D-22~D-28 容错与生命周期）
  </read_first>

  <behavior>
    构造与指令查找（D-06/D-07）:
    - new Binding(el, "text", "user.name", store, directiveRegistry) 构造成功
    - 构造时内部调用 directiveRegistry.get("text") 查找指令对象，存为实例字段
    - 当 directiveRegistry 中无该指令时，记 warn 日志并标记为无效（不抛错，容错 D-22~D-24）

    计算对象创建（D-08/D-29/D-30）:
    - 构造时内部调用 ExpressionParser.parse(expression, { throws: false }) 获取 ParsedExpression（容错，D-22~D-24）
    - 解析失败（返回 null）时记 warn 并标记无效，不创建计算对象
    - 解析成功时调用 store.computedObjects.create((scope) => parsed.evaluate(scope)) 创建同步计算对象
    - getter 内对 scope.user.name 的读取被 AutoStore 自动追踪为依赖（D-29）

    监听与触发更新（D-09/D-10）:
    - bind() 调用 directive.init(el, binding, store)（若 init 存在，保留返回的清理函数）
    - bind() 用计算对象当前值立即调用一次 directive.update（保证初始渲染）
    - 计算对象依赖变化时（computed.watch 触发），调用 directive.update(el, newValue)
    - 无效 Binding（指令未找到或表达式非法）调用 bind() 时不做任何事（静默跳过）

    生命周期清理（D-25~D-28，幂等）:
    - destroy() 调用 directive.destroy(el)（若存在，try/catch 错误记 log error 不中断）
    - destroy() 调用 computed 所在监听的 watcher.off() 解绑监听
    - destroy() 调用 store.computedObjects.delete(computed.id) 移除计算对象（detach + Map 移除）
    - destroy() 幂等：重复调用不报错、不重复清理
    - destroyed 后计算对象变化不再触发 directive.update
  </behavior>

  <action>
    创建 src/Binding.ts，导出 `class Binding`（对应 D-05~D-11 简化设计）：

    1. 构造函数 `constructor(el: HTMLElement, directiveName: string, expression: string, store: AnyAutoStore, directiveRegistry: DirectiveRegistry)`（D-06）。将 el/directiveName/expression/store 存为实例字段（public readonly el/expression；private store/directiveName）。

    2. 构造时执行内部初始化（D-07/D-08）：
       a. `this.directive = directiveRegistry.get(directiveName)`。若 undefined：`store.log("指令未注册: " + directiveName, "warn")`（D-23），标记 `this.#valid = false`，return（不继续创建计算对象）。
       b. `this.parsed = ExpressionParser.parse(expression, { throws: false })`（D-22 容错）。若返回 null：`store.log("表达式解析失败: " + expression, "warn")`（D-23），标记 `this.#valid = false`，return。
       c. `this.computed = store.computedObjects.create((scope) => this.parsed!.evaluate(scope))`（D-08/D-02）。切勿手工传 depends 数组——让 AutoStore 通过 getter 内 scope 读取自动收集依赖（sync.ts collectDependencies 机制，D-29）。
       d. 标记 `this.#valid = true`。

    3. bind() 方法（D-09）：
       - 若 `!this.#valid` 直接 return（无效绑定静默跳过，D-24）。
       - 调用 `this.directive.init(this.el, <DirectiveBinding>{directive: this.directiveName, expression: this.expression, element: this.el, parsed: this.parsed}, this.store)`（若 init 存在），保留返回的清理函数到 `this.#initCleanup`。
       - 建立 watch 监听（D-09）：`this.#watcher = this.computed.watch(() => { this.#invokeUpdate() })`。computed.watch 在 ObserverObject 中实现，监听计算对象值路径的变化（getValueWatchPath）。回调触发时调用 #invokeUpdate。
       - 首次渲染：立即用 `this.computed.value`（当前计算结果）调用一次 `this.directive.update?.(this.el, <binding>, this.store)` 或直接 `this.#invokeUpdate()`（保证初始渲染）。注意：update 方法签名是 (el, binding, store)——调用时传完整 binding 对象。

    4. 私有 `#invokeUpdate()`：若已 destroyed 或无效则 return；否则调用 `this.directive.update?.(this.el, this.#getBinding(), this.store)`（D-10：更新由指令执行，Binding 只负责触发）。

    5. destroy() 方法（D-25~D-28，幂等）：
       - 若 `this.#destroyed` 直接 return（幂等）。
       - 解绑监听：`this.#watcher?.off()`。
       - 调用 `this.directive.destroy?.(this.el)`（try/catch，错误记 `this.store.log(..., "error")` 不中断，D-26）。
       - 调用 `this.#initCleanup?.()`（init 返回的清理函数，若存在）。
       - 移除计算对象：`this.store.computedObjects.delete(this.computed.id)`（若 computed 存在，D-27）。
       - 标记 `this.#destroyed = true`。

    6. 暴露 getter：`get valid(): boolean`（供 BindingManager/AutoTemplate 判断是否跳过）、`get isDestroyed(): boolean`。

    创建 src/__tests__/Binding.test.ts：用真实 AutoStore 实例（`new AutoStore({ user: { name: "zhang" } })`）+ 真实 DirectiveRegistry（注册一个 mock 指令：init/update/destroy 用 bun:test 的 spy 手写对象）+ 真实 ExpressionParser。验证 <behavior>：
    - 构造成功 + bind 后 init 被调用 + update 被立即调用一次（初始值 "zhang"）
    - 修改 store.state.user.name = "wang" 后，directive.update 被再次调用且收到新值（D-29 自动追踪生效）
    - 未注册指令名构造 → valid=false，bind() 无操作，不抛错
    - 非法表达式 "evil()" 构造（throws:false）→ valid=false，bind() 无操作
    - destroy() 后再改 state，update 不再被调用
    - destroy() 重复调用不报错（幂等）
    - destroy() 后 store.computedObjects 不再含该 computed.id

    切勿：不要创建 ExpressionBridge 类（D-11 已移除）。不要在此创建 BindingManager（Plan 02-02 负责）。不要修改 AutoTemplate.ts。不要手工管理依赖数组（让 AutoStore 自动追踪）。
  </action>

  <verify>
    <automated>cd packages/template && bun test src/__tests__/Binding.test.ts && bunx tsc --noEmit</automated>
  </verify>

  <done>
    - src/Binding.ts 导出 `class Binding`，构造函数签名为 (el, directiveName, expression, store, directiveRegistry)（D-06）
    - Binding 内部调用 directiveRegistry.get(directiveName) 查找指令（D-07，grep "directiveRegistry.get" 或 ".get(directiveName)" 命中）
    - Binding 内部调用 store.computedObjects.create() 创建计算对象（D-08，grep "computedObjects.create" 命中）
    - Binding 内部调用 computed.watch() 监听变化（D-09，grep ".watch(" 命中）
    - 值变化后 directive.update 被调用（D-10）
    - 未注册指令 / 非法表达式时 valid=false 且不抛错（D-22~D-24 容错）
    - destroy() 幂等、调用 directive.destroy + computedObjects.delete、解绑 watcher（D-25~D-28）
    - TypeScript 编译无错误
  </done>
</task>

</tasks>

<verification>
- cd packages/template && bun test src/__tests__/ExpressionParser.test.ts src/__tests__/Binding.test.ts 全部通过
- cd packages/template && bunx tsc --noEmit 无类型错误
- ExpressionParser 无 eval/new Function（grep 验证）
- Binding 内部完成"查找指令+创建计算对象+监听+触发更新"四步（D-05~D-11 简化设计）
- EXPR-01/EXPR-02/EXPR-03 验收标准由单元测试覆盖
- 无 ExpressionBridge 文件（D-11 已移除该中间层）
</verification>

<success_criteria>
- ExpressionParser 能安全解析标识符访问与字面量，拒绝所有 EXPR-03 列举的不安全语法，容错模式按 options.throws 控制
- 简化版 Binding 类直接在内部完成指令查找、计算对象创建、监听建立、更新触发，不再依赖 ExpressionBridge
- Binding 利用 AutoStore computed 自动依赖追踪与缓存（D-29/D-30）
- 容错模式工作：未注册指令或非法表达式不中断，记 warn 跳过（D-22~D-24）
- 生命周期完整清理且幂等（D-25~D-28）
- 所有单元测试通过，TypeScript 严格模式编译通过
</success_criteria>

<output>
Create `.planning/phases/02-expression-system/02-01-SUMMARY.md` when done
</output>
