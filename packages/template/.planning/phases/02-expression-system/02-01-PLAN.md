---
phase: 02-expression-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types.ts
  - src/ExpressionParser.ts
  - src/ExpressionBridge.ts
  - src/Binding.ts
  - src/__tests__/ExpressionParser.test.ts
  - src/__tests__/ExpressionBridge.test.ts
  - src/__tests__/Binding.test.ts
autonomous: true
requirements:
  - EXPR-01
  - EXPR-02
  - EXPR-03
tags: [expression, parser, security, binding, computed]

must_haves:
  truths:
    - "ExpressionParser 能解析标识符访问表达式（如 user.name）并返回求值函数和依赖路径"
    - "ExpressionParser 能解析字面量表达式（'hello', 123, true, false）且字面量不产生依赖"
    - "ExpressionParser 拒绝不安全表达式（函数调用 new Function、赋值 =、成员构造等）并抛出错误"
    - "ExpressionBridge.create() 能将表达式字符串转为 AutoStore 计算对象，依赖由 AutoStore 自动追踪"
    - "Binding 类持有 {el, directive, computed} 三元组并暴露 destroy() 清理接口"
  artifacts:
    - path: "src/ExpressionParser.ts"
      provides: "安全的表达式解析与求值器（标识符访问 + 字面量 + 安全校验）"
      exports: ["ExpressionParser", "ExpressionParseError"]
      min_lines: 120
    - path: "src/ExpressionBridge.ts"
      provides: "表达式到 AutoStore 计算对象的桥接（D-01~D-04, D-19~D-21）"
      exports: ["ExpressionBridge"]
      min_lines: 60
    - path: "src/Binding.ts"
      provides: "连接 {el, directive, computed} 的绑定类骨架（D-05~D-08, D-25~D-28）"
      exports: ["Binding"]
      min_lines: 60
    - path: "src/types.ts"
      provides: "扩展 DirectiveUpdater、ExpressionOptions、Binding 类型"
  key_links:
    - from: "src/ExpressionBridge.ts"
      to: "store.computedObjects.create()"
      via: "调用 AutoStore 计算对象工厂"
      pattern: "computedObjects\\.create"
    - from: "src/Binding.ts"
      to: "src/ExpressionBridge.ts"
      via: "持有 bridge 返回的 computed 对象"
      pattern: "ExpressionBridge|computed"
---

<objective>
实现 Phase 02 表达式系统的第一层：安全的表达式解析器、表达式到 AutoStore 计算属性的桥接、以及 Binding 绑定类骨架。

Purpose: CONTEXT.md D-01~D-04 锁定"表达式 = AutoStore 计算属性"——复用 AutoStore 的依赖追踪与缓存，不另起解析引擎。但 EXPR-01/EXPR-02/EXPR-03 要求支持标识符访问、字面量、并拒绝不安全语法。本计划通过"在计算对象 getter 内调用受限求值器"调和两者：AutoStore 负责依赖追踪与重算触发，ExpressionParser 负责安全求值。Binding 类（D-05~D-08）作为连接 {el, directive, computed} 的三元组骨架，在 Plan 02-02 由 BindingManager 编排。

Output: ExpressionParser.ts、ExpressionBridge.ts、Binding.ts、types.ts 扩展、以及三组单元测试。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-expression-system/02-CONTEXT.md
@.planning/phases/01-core-infrastructure/01-01-SUMMARY.md

<interfaces>
<!-- Phase 01 已建立的契约，executor 直接使用，无需探索代码库 -->

From src/types.ts (Phase 01 现有，本计划将扩展):
```typescript
export type AnyAutoStore = AutoStore<any, any>;

export interface Directive {
    name: string;
    priority: number;
    init(el: HTMLElement, binding: DirectiveBinding, store: AnyAutoStore): void | (() => void);
    update?(el: HTMLElement, binding: DirectiveBinding, store: AnyAutoStore): void;
    destroy?(el: HTMLElement): void;
}

export interface DirectiveBinding {
    directive: string;
    expression: string;
    element: HTMLElement;
    parsed: ParsedExpression | null;   // 本计划将填充此字段
}

export interface ParsedExpression {
    dependencies: string[];
    evaluate(scope: any): any;
}

export interface RenderOptions {
    debug?: boolean;
}
```

From AutoStore core (../../core/src/computed/computedObjects.ts) — 动态创建计算对象的真实签名:
```typescript
class ComputedObjects<State> extends Map<string, ComputedObject> {
    // 同步计算对象：getter 内对 scope 的读取会被 AutoStore 自动追踪为依赖
    create<Value, Scope>(
        getter: ComputedGetter<Value, Scope>,
        options?: SyncComputedOptions<Value, Scope>,
    ): SyncComputedObject<Value, Scope>;
}
// SyncComputedObject 通过 onInitial() -> collectDependencies() 自动收集依赖，
// 依赖变化时 onDependsChange -> run() 自动重算，无需手工管理。
// 计算结果变化通过 store 事件 "computed:done" 或 watch 计算路径感知。
```

Phase 01 AutoTemplate.#setupWatcher() 当前是空 TODO，本计划不修改 AutoTemplate（由 Plan 02-02 集成）。
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 扩展类型定义并实现 ExpressionParser 安全求值器</name>
  <files>src/types.ts, src/ExpressionParser.ts, src/__tests__/ExpressionParser.test.ts</files>

  <read_first>
    - src/types.ts（当前 ParsedExpression / DirectiveBinding 定义，扩展而非重写）
    - .planning/phases/02-expression-system/02-CONTEXT.md（D-01~D-04 表达式=计算属性、D-22~D-24 容错模式）
    - ../../core/src/computed/sync.ts（sync 计算对象如何通过 scope 读取收集依赖——求值器需返回值供 getter 使用）
  </read_first>

  <behavior>
    标识符访问（EXPR-01）:
    - parseAndEval("user.name", {user:{name:"zhang"}}) 返回 "zhang"
    - parseAndEval("items.length", {items:[1,2,3]}) 返回 3
    - parseAndEval("a.b.c", {a:{b:{c:42}}}) 返回 42
    - getDependencies("user.name") 返回 ["user.name"]（点号路径合并为完整依赖路径）
    - getDependencies("a.b.c") 返回 ["a.b.c"]
    - 依赖变化时由 AutoStore 计算对象触发重算（在 Task 2 ExpressionBridge 中验证）

    字面量（EXPR-02）:
    - parseAndEval("'hello'", {}) 返回 "hello"（单引号字符串）
    - parseAndEval('"world"', {}) 返回 "world"（双引号字符串）
    - parseAndEval("123", {}) 返回 123（数字）
    - parseAndEval("true", {}) 返回 true
    - parseAndEval("false", {}) 返回 false
    - getDependencies("'hello'") 返回 []（字面量不产生依赖）
    - getDependencies("123") 返回 []

    安全性（EXPR-03）——以下全部抛出 ExpressionParseError:
    - 含 "function" 关键字的函数表达式
    - 含 "=>" 箭头函数
    - 含 "new " 构造调用
    - 含赋值 "="（除 "==" 和 "===" 和 "!=" 和 "!=="）
    - 含 ";" 多语句
    - 含 "(" 或 ")" 函数调用语法
    - 含 "[" 或 "]" 索引访问（v1 限制，用点号路径）
    - parse("") 抛出 ExpressionParseError（空表达式）

    容错（D-22~D-24）:
    - parse 提供 throws 选项：当 { throws: false } 时返回 null 而非抛出（供 Bridge 容错使用）
  </behavior>

  <action>
    1. 扩展 src/types.ts：
       - 新增 `export class ExpressionParseError extends Error`，构造函数接收 message，name 固定为 "ExpressionParseError"。
       - 新增接口 `DirectiveUpdater`（对应 CONTEXT D-13/D-14 更新器对象）：`{ update(el: HTMLElement, value: any): void; destroy?(el: HTMLElement): void }`。
       - 新增类型 `ExpressionOptions`：`{ throws?: boolean; dependencies?: string[] }`（对应 D-19/D-20 options）。
       - 保留现有 ParsedExpression 接口不变（dependencies + evaluate），本任务产出符合该接口的对象。

    2. 创建 src/ExpressionParser.ts，导出 `ExpressionParser` 类（单例风格，提供静态方法或实例方法均可）：
       - 方法 `parse(expr: string, options?: ExpressionOptions): ParsedExpression | null`：返回 { dependencies, evaluate }。
       - 方法 `getDependencies(expr: string): string[]`：提取标识符路径。
       - 方法 `parseAndEval(expr: string, scope: any, options?: ExpressionOptions): any`：便捷方法，解析并求值。
       - 实现【受限求值器】策略，不使用 eval/new Function（这会违反 EXPR-03）：
         a. 字面量识别：用正则匹配单/双引号字符串、纯数字、true/false。字面量 dependencies 为 []。
         b. 标识符路径识别：用正则 `/^[A-Za-z_$][\w$]*(?:\.[A-Za-z_$][\w$]*)*$/` 校验整串仅为点号路径。
         c. 路径求值：evaluate(scope) 按 "." 拆分路径，逐层读取 scope。缺失中间节点返回 undefined（不抛错）。
         d. 安全校验（EXPR-03）：在 parse 入口先跑 rejectUnsafe(expr)，用正则/blacklist 检测：/\bfunction\b/、/=>/、/\bnew\s+/、/[;]/、/[\(\)\[\]]/、以及赋值 /(?<![=!<>])=(?![=])/（排除 == === != <= >=）。命中即抛 ExpressionParseError。
       - 容错选项（D-22~D-24）：options.throws === false 时，parse 捕获错误返回 null（不抛出），由调用方决定是否 warn。默认 throws=true 保持原抛出语义。

    3. 创建 src/__tests__/ExpressionParser.test.ts，用 bun:test 的 describe/it/expect 覆盖 <behavior> 中所有断言（标识符、字面量、安全性、容错）。测试名称使用中文（遵循项目约定）。

    切勿：不要在 ExpressionParser 内调用 AutoStore 或 computedObjects（本任务纯解析层，AutoStore 集成在 Task 2）。不要修改 AutoTemplate.ts / TemplateScanner.ts（Plan 02-02 负责）。
  </action>

  <verify>
    <automated>cd packages/template && bun test src/__tests__/ExpressionParser.test.ts</automated>
  </verify>

  <acceptance_criteria>
    - src/ExpressionParser.ts 存在且 `export class ExpressionParser` 与 `export class ExpressionParseError`
    - src/types.ts 包含 `interface DirectiveUpdater` 与 `type ExpressionOptions` 与 `class ExpressionParseError`（若放在 types.ts）或在 ExpressionParser.ts 中导出
    - bun test 通过，覆盖：user.name→"zhang"、items.length→3、'hello'→"hello"、123→123、true→true、字面量 dependencies 为 []、标识符 dependencies 含完整点号路径
    - 安全性用例全部抛出 ExpressionParseError：含 function/=>/new/;/()[]/赋值=
    - parse("") 抛出 ExpressionParseError；parse(x, {throws:false}) 对非法输入返回 null
    - grep -c "new Function\|eval(" src/ExpressionParser.ts 输出 0（禁止动态代码执行）
    - TypeScript 编译无错误：cd packages/template && bunx tsc --noEmit
  </acceptance_criteria>

  <done>ExpressionParser 能安全解析标识符访问与字面量，拒绝所有 EXPR-03 列举的不安全语法，容错模式按 options.throws 控制，单元测试全部通过。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 实现 ExpressionBridge 桥接表达式到 AutoStore 计算对象</name>
  <files>src/ExpressionBridge.ts, src/__tests__/ExpressionBridge.test.ts</files>

  <read_first>
    - src/ExpressionParser.ts（Task 1 产出，调用其 parse 方法）
    - src/types.ts（AnyAutoStore、ExpressionOptions）
    - ../../core/src/computed/computedObjects.ts（create 真实签名：create(getter, options?)）
    - ../../core/src/computed/sync.ts（SyncComputedObject 如何通过 getter 内 scope 读取自动收集依赖——D-29/D-30）
    - .planning/phases/02-expression-system/02-CONTEXT.md（D-01~D-04, D-16~D-18, D-19~D-21, D-22~D-24）
  </read_first>

  <behavior>
    - ExpressionBridge.create("user.name", store) 返回对象 { computed, dependencies, cleanup }
    - 当 store.state.user.name 变化时，computed.value 自动更新（AutoStore 依赖追踪生效，D-29/D-30）
    - ExpressionBridge.create("'hello'", store) 返回 dependencies 为 []（字面量无依赖）
    - ExpressionBridge.create("evil()", store, { throws: false }) 返回 null 并触发 store.log(..., "warn")（D-22~D-24 容错：不抛出、记 warn、返回无效结果）
    - ExpressionBridge.create("evil()", store) （默认 throws:true）抛出 ExpressionParseError
    - cleanup() 调用后从 store.computedObjects 移除该计算对象（D-21 自定义清理函数）
    - 每个 create 调用创建独立计算对象（D-16 不复用）
  </behavior>

  <action>
    创建 src/ExpressionBridge.ts，导出 `class ExpressionBridge`，提供 `create(expr, store, options?)` 方法（对应 D-19~D-21）：

    1. 调用 ExpressionParser.parse(expr, { throws: options?.throws ?? true })。若返回 null（容错模式命中非法表达式），执行 `store.log("表达式解析失败: " + expr, "warn")`（D-23）并返回 null（D-22 容错：不终止调用方）。

    2. 用 `store.computedObjects.create(getter)` 创建同步计算对象（D-02）。getter 签名为 `(scope) => parsed.evaluate(scope)`——AutoStore 会在首次运行时通过 scope 读取自动收集依赖（见 sync.ts collectDependencies）。切勿手工传 depends 数组（让 AutoStore 自动追踪，除非 options.dependencies 显式提供，此时作为补充依赖传入 create 的第二参数）。

    3. 返回对象 `{ computed, dependencies: parsed.dependencies, cleanup }`：
       - computed: store.computedObjects.create 的返回值（SyncComputedObject）。
       - dependencies: 来自 ExpressionParser.getDependencies 的路径数组（静态分析结果，供调试/日志；真实依赖以 AutoStore 收集为准）。
       - cleanup: 闭包函数，调用 `store.computedObjects.delete(computed.id)`（见 computedObjects.ts delete 方法）移除计算对象（D-21）。

    4. 每个 create 调用独立创建（D-16/D-17），不做缓存/复用。

    创建 src/__tests__/ExpressionBridge.test.ts：用真实 AutoStore 实例（`import { AutoStore } from "autostore"` 或项目惯用的 createStore）构造 `store = new AutoStore({ user: { name: "zhang" } })`，验证 <behavior>。修改 state 后断言 computed.value 更新。容错用例验证返回 null 且不抛出。

    切勿：不要在 bridge 内自己实现依赖追踪（D-29 明确由 AutoStore 负责）。不要修改 ExpressionParser.ts。
  </action>

  <verify>
    <automated>cd packages/template && bun test src/__tests__/ExpressionBridge.test.ts</automated>
  </verify>

  <acceptance_criteria>
    - src/ExpressionBridge.ts 导出 `class ExpressionBridge`，含 create(expr, store, options?) 方法
    - create 返回值结构为 { computed, dependencies, cleanup } 三字段（D-19~D-21）
    - 测试证明：store.state.user.name 变化后 computed.value 更新（AutoStore 依赖追踪生效）
    - 字面量表达式 create("'hello'", store).dependencies 深度等于 []
    - create("evil()", store, { throws: false }) 返回 null 且不抛出
    - create("evil()", store) 抛出 ExpressionParseError
    - cleanup() 后 store.computedObjects 不再含该 computed.id
    - TypeScript 编译无错误：cd packages/template && bunx tsc --noEmit
  </acceptance_criteria>

  <done>ExpressionBridge 能将表达式字符串桥接为 AutoStore 同步计算对象，依赖由 AutoStore 自动追踪，容错模式按 CONTEXT D-22~D-24 工作。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 实现 Binding 绑定类骨架</name>
  <files>src/Binding.ts, src/__tests__/Binding.test.ts</files>

  <read_first>
    - src/types.ts（Directive、DirectiveBinding、DirectiveUpdater——Task 1 已扩展）
    - src/ExpressionBridge.ts（Task 2 产出，Binding 持有其返回的 computed）
    - .planning/phases/02-expression-system/02-CONTEXT.md（D-05~D-08 三元组、D-13~D-15 更新器对象、D-25~D-28 生命周期）
  </read_first>

  <behavior>
    - new Binding(el, directive, computed, updater) 构造成功，实例含 el/directive/computed 字段
    - Binding.bind() 调用 directive.init(el, binding, store)（若 init 存在）
    - 当 computed 值变化时，调用 updater.update(el, value)（D-08/D-15：Binding 触发，指令负责 DOM 更新）
    - Binding.destroy() 调用 directive.destroy(el)（若存在）和 updater.destroy(el)（若存在）和 computed 清理（D-25~D-28）
    - Binding.destroy() 幂等：重复调用不报错、不重复清理
    - destroyed 后再次触发更新不再调用 updater.update（已解绑监听）
  </behavior>

  <action>
    创建 src/Binding.ts，导出 `class Binding`（对应 D-05~D-08）：

    1. 构造函数 `constructor(el: HTMLElement, directive: Directive, computed: SyncComputedObject, updater: DirectiveUpdater, store: AnyAutoStore)`。将 el/directive/computed/updater/store 存为实例字段（public readonly el/directive/computed；private updater/store）。

    2. 监听机制（D-07）：因为 AutoStore SyncComputedObject 在依赖变化时自动重算并通过 store 事件 "computed:done" 广播，Binding 通过订阅该计算对象的变化感知更新。具体方式：在 bind() 中调用 `this.computed.on("after", callback)`（SyncComputedObject 继承 ObserverObject，提供 before/after hook——见 sync.ts _runHook("after")）或等价的 watch/computedObjects 监听。callback 接收 (value, error)，调用 `this.updater.update(this.el, value)`。若 after hook 不可用，回退为 `this.store.watch(this.computed.path, (operate) => { this.updater.update(this.el, this.computed.value) })` 并保存返回的 watcher 用于 destroy 时 off()。executor 需读 computedObject/observer 源码确认可用 API，优先 after hook。

    3. bind() 方法：先调用 directive.init(el, binding, store)（若 init 存在，保留其返回的清理函数供 destroy 使用），再建立上述监听。首次绑定立即用 computed 当前值调用一次 updater.update（保证初始渲染）。

    4. destroy() 方法（D-25~D-28）：若已 destroyed 直接返回（幂等）。解绑监听（off after hook 或 watcher.off()）。调用 directive.destroy(el)（try/catch，错误记 store.log error 不中断）。调用 updater.destroy(el)（若存在，try/catch）。不在此调用 computed 的销毁（由 ExpressionBridge.cleanup 负责，职责分离；Binding 只解绑自己的监听）。标记 #destroyed = true。

    创建 src/__tests__/Binding.test.ts：用 mock directive（jest 风格手写对象，bun:test 兼容）、mock updater、真实 AutoStore + ExpressionBridge 构造 computed。验证 init 被调用、值变化触发 update、destroy 幂等、destroyed 后不再 update。

    切勿：不要在此创建 BindingManager（Plan 02-02）。不要修改 AutoTemplate.ts。
  </action>

  <verify>
    <automated>cd packages/template && bun test src/__tests__/Binding.test.ts</automated>
  </verify>

  <acceptance_criteria>
    - src/Binding.ts 导出 `class Binding`
    - Binding 构造函数接收 (el, directive, computed, updater, store) 五参数
    - bind() 调用 directive.init 且用 computed 当前值立即调用一次 updater.update
    - computed 依赖变化后 updater.update(el, newValue) 被调用（D-08）
    - destroy() 调用 directive.destroy(el) 与 updater.destroy(el)（若存在）
    - destroy() 幂等：第二次调用不抛错且不重复调用 destroy
    - destroyed 后 computed 变化不再触发 updater.update
    - TypeScript 编译无错误：cd packages/template && bunx tsc --noEmit
  </acceptance_criteria>

  <done>Binding 类作为 {el, directive, computed} 三元组连接器，能在值变化时触发指令更新器，destroy 时完整清理且幂等。</done>
</task>

</tasks>

<verification>
- cd packages/template && bun test src/__tests__/ 全部通过
- cd packages/template && bunx tsc --noEmit 无类型错误
- ExpressionParser 无 eval/new Function（grep 验证）
- EXPR-01/EXPR-02/EXPR-03 验收标准由单元测试覆盖
</verification>

<success_criteria>
- ExpressionParser 能解析标识符访问与字面量，拒绝不安全语法
- ExpressionBridge 能桥接到 AutoStore 计算对象且依赖自动追踪
- Binding 类骨架完成，后续 Plan 02-02 的 BindingManager 可直接编排
- 所有单元测试通过，TypeScript 严格模式编译通过
</success_criteria>

<output>
Create `.planning/phases/02-expression-system/02-01-SUMMARY.md` when done
</output>
